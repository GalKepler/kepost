PARCELLATIONS_DESCRIPTIONS = """
For each combination of parcellation atlas and tensor-derived metric,
several summary statistics were calculated for each region of interest (ROI).
For a list of all sumary statistics, see *Regional Summary Statistics*.
"""

SUMMARY_STATISTICS = """
**Regional Summary Statistics**

* Mean.
* Z-filtered mean (mean after removal of voxels with Z-score > 3).
* Quantile-filtered mean (mean after removal of voxels outside the 10th and 90th quantiles).
* IQR-filtered mean (mean after removal of voxels outside the IQR range).
* Median
* Median Absolute Deviation (MAD)-filtered median (median after removal of voxels with MAD > 3).
"""
